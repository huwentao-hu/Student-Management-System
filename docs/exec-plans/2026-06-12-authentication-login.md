# 执行记录：账号角色与登录

日期：2026-06-12  
状态：已完成

## 完成内容

- 确认管理员、教师、学生三个角色及基础权限方向。
- 新增 `user_accounts` 和 `auth_tokens` 数据表迁移。
- 实现 BCrypt 密码哈希配置。
- 实现用户名密码登录接口。
- 实现高强度随机令牌生成、哈希保存和过期时间。
- 实现可选的启动管理员创建。
- 登录失败统一返回 `401 Unauthorized`。

## 验证结果

- `backend\mvnw.cmd test`：通过，6 个测试，0 失败。
- H2 上 Flyway V1、V2：执行成功。
- PostgreSQL 上 Flyway V2：执行成功。
- 真实 PostgreSQL 管理员登录：成功。
- PostgreSQL 中密码为 BCrypt 哈希：确认。
- PostgreSQL 中保存的令牌为 64 位 SHA-256 哈希，不是原始令牌：确认。

## 实施中修正

- 测试配置文件覆盖主配置文件，导致认证配置未加载；已在测试配置中显式加入认证参数。
