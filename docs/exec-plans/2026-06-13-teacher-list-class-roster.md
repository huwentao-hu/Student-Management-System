# 执行计划：教师列表与班级花名册

日期：2026-06-13  
状态：已完成

## 目标

补齐班级管理前端依赖的教师选择和当前班级学生名单能力，使班级管理形成完整流程。

## 完成内容

- 新增仅管理员可查询的启用教师账号列表接口。
- 新增管理员和教师可查询的班级当前花名册接口。
- 花名册只包含 `endDate` 为空的当前分班学生，并按姓名排序。
- 新增班级前端使用教师下拉选择班主任。
- 班级详情展示当前花名册并可进入学生详情。
- 未新增数据库表或 Flyway 迁移。

## 验证结果

- 后端集成测试：44 个通过，0 失败。
- 前端 `npm run lint`：通过。
- 前端 `npm run build`：通过。
- Playwright 模拟 API 流程：教师下拉和当前花名册均通过。
- 真实 PostgreSQL：启用教师和当前分班花名册查询成功。
