# 执行记录：学生接口鉴权与授权

日期：2026-06-12  
状态：已完成

## 完成内容

- 新增 Bearer Token 统一认证拦截器。
- 验证令牌哈希、过期时间和账号启用状态。
- 新增统一 `401 Unauthorized` 和 `403 Forbidden` 响应。
- 管理员可新增和查询所有学生。
- 教师可查询所有学生，但不可新增。
- 学生只能查询自己的单条档案，不能访问全体列表。

## 验证结果

- `backend\mvnw.cmd test`：通过，9 个测试，0 失败。
- 覆盖未登录、管理员、教师、学生本人和学生越权场景。
- 真实 PostgreSQL 管理员 Bearer Token 查询：成功。
- 真实 PostgreSQL 未携带令牌查询：返回 `401`。
- 本功能未修改数据库结构。

## 实施中修正

- 权限测试生成的学号最初超过数据库 32 字符限制，已缩短测试数据并保留数据库限制。
