# 执行计划：过期登录令牌清理

日期：2026-06-12  
状态：已完成

## 目标

实现过期登录令牌的数据库条件删除、定期任务和管理员手动清理接口。

## 执行步骤

1. 新增过期令牌条件删除查询。
2. 新增可配置定期清理任务。
3. 新增管理员手动清理接口。
4. 补充权限、删除数量和有效令牌保留测试。
5. 使用真实 PostgreSQL 验证。
6. 更新所有相关文档和技术债状态。

## 验收条件

- 清理操作只删除已过期令牌。
- 管理员可手动清理并获得删除数量。
- 教师和学生不能手动清理令牌。
- 定期清理任务可通过配置调整周期。
- 自动化测试和真实 PostgreSQL 验证通过。

## 完成内容

- 新增数据库过期令牌条件删除查询。
- 新增默认每小时执行的可配置定期清理任务。
- 新增管理员手动清理接口并返回删除数量。
- 测试环境延长调度延迟，避免定期任务干扰集成测试。
- 未新增数据库表或 Flyway 迁移。

## 验证结果

- `backend\mvnw.cmd test`：通过，42 个测试，0 失败。
- H2 上 Flyway V1 至 V9：执行成功。
- 真实 PostgreSQL 手动清理：删除 1 条过期令牌。
- 执行清理的有效管理员令牌在清理后仍可继续访问。
